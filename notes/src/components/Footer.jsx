const Footer = () => {
    const footerStyle = {
        color: 'green',
        fontStyle: 'italic',
        fontSize: 16
    };

    return (
        <footer style={ footerStyle }>
            <em>Note app</em>
        </footer>
    );
};

export default Footer;